# Los scripts de esta carpeta se ejecutan solo por CLI desde el cron.
# Se bloquea cualquier acceso por web.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
